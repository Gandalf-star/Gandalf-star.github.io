import 'package:flutter/material.dart';

Widget getPlatformImage(String url,
    {double? width, double? height, BoxFit? fit}) {
  throw UnsupportedError(
      'Cannot create a platform image without the packages.');
}
